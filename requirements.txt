# FoodStore — dependencias del backend (API FastAPI)
# Uso: crear un venv, activarlo y desde la raíz del repo:
#   pip install -r requirements.txt
# Luego seguí el README: cd backend, .env, seed, uvicorn.

fastapi>=0.115.0
httpx>=0.27.0
uvicorn[standard]>=0.32.0
sqlmodel>=0.0.22
psycopg2-binary>=2.9.9
pydantic-settings>=2.6.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
slowapi>=0.1.9
email-validator>=2.2.0
alembic>=1.13.0
mercadopago>=2.3.0
bcrypt>=4.0.0

# Opcional — tests (pytest + cobertura)
pytest>=8.3.0
pytest-cov>=5.0.0
